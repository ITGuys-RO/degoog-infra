#!/usr/bin/env bash
set -euo pipefail

echo "Web access in this session is routed through a self-hosted degoog instance (exit IP in Romania). Use MCP tools \`mcp__degoog__degoog_search\` and \`mcp__degoog__degoog_fetch\` for all web searches and URL fetches; the built-in WebSearch and WebFetch tools are blocked."
